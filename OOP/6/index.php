<?php
    // Константы - это статические свойства и методы
    require_once 'classes/Car4.php';

    $car1 = new Car4();
    $car2 = new Car4();

    echo $car1->getCarInfo();
    echo $car2->getCarInfo();

    echo Car4::$countCar;
    echo Car4::getCount();

//    echo $car1::test(); // Не статические методы можно вызывать статически. Но не желательно, но возможно.
    //Вызывает ошибку!

    echo $car1->getPrototypeInfo();

    echo Car4::TEST_CAR;

    echo Car4::class; // имя класса. Возможность полезна, когда работаем с пространством имен

    // Статические свойства и методы, константы - они принадлежат классу, не объекту поэтому к ним обращаться
    // нужно в контексте имено класса, если мы вне класса, то через имя класса.
    // Если внутри класса, то через ключевое слово self